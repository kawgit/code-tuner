import math

x = $$$2$$$ #2 is surrounded by the DENOTATION symbols defined in main.py, so it will be brute forced to maximize printed score

print(math.cos(x - 5) * math.exp(-math.pow(math.sin(x)+x*math.cos(x), 2)))